CREATE TABLE "ADMIN"."SERVICE_BULLETIN" 
   (	"SB_PARTSERVICE" VARCHAR2(20 BYTE) COLLATE "USING_NLS_COMP", 
	"USER_ID_REGISTER" NUMBER, 
	"SB_DTREGISTER" TIMESTAMP (6) DEFAULT SYSDATE, 
	"SB_ID" NUMBER, 
	"SB_NAME" VARCHAR2(15 BYTE) COLLATE "USING_NLS_COMP"
   )  DEFAULT COLLATION "USING_NLS_COMP" ;

CREATE UNIQUE INDEX "ADMIN"."PK_SB_ID" ON "ADMIN"."SERVICE_BULLETIN" ("SB_ID") 
  ;

ALTER TABLE "ADMIN"."SERVICE_BULLETIN" MODIFY ("SB_ID" NOT NULL ENABLE);

ALTER TABLE "ADMIN"."SERVICE_BULLETIN" ADD CONSTRAINT "PK_SB_ID" PRIMARY KEY ("SB_ID")
  USING INDEX  ENABLE;

ALTER TABLE "ADMIN"."SERVICE_BULLETIN" MODIFY ("SB_DTREGISTER" NOT NULL ENABLE);

ALTER TABLE "ADMIN"."SERVICE_BULLETIN" ADD CONSTRAINT "FK_USER_ID_REGISTER_SB" FOREIGN KEY ("USER_ID_REGISTER")
	  REFERENCES "ADMIN"."USERS" ("USER_ID") ENABLE;

CREATE OR REPLACE EDITIONABLE TRIGGER "ADMIN"."TRG_SEQ_SB_ID" 
BEFORE INSERT ON SERVICE_BULLETIN
FOR EACH ROW
BEGIN
  SELECT SB_ID_SEQ.NEXTVAL
  INTO :new.SB_ID
  FROM dual;
END;
/
ALTER TRIGGER "ADMIN"."TRG_SEQ_SB_ID" ENABLE;

